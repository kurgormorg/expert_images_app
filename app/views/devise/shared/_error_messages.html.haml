- if resource.errors.any?
  .flash.flash-danger.form-errors
    %p= t("errors.messages.not_saved", count: resource.errors.count, resource: resource.class.model_name.human)
    %ul
      - resource.errors.full_messages.each do |message|
        %li= message
